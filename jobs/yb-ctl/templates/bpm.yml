---
processes:
  - name: yb-ctl
    executable: /var/vcap/packages/yugabyte/bin/yb-ctl
    persistent_disk: true
    args:
      - start
      - --binary_dir=/var/vcap/packages/yugabyte/bin/
      - --data_dir=/var/vcap/store/yugabyte/
      - --v=3
      - --verbose

    capabilities:
      - NET_BIND_SERVICE

    # unsafe:
    #   priviledged: true

#!/usr/bin/env bash

set -eux

cd ${BOSH_INSTALL_TARGET}
mkdir jre && tar zxvf ${BOSH_COMPILE_TARGET}/openjdk8/*.tar.gz -C jre
if [[ $? != 0 ]]; then
  echo "Cannot unpack JRE"
  exit 1
fi

# latest JRE release didn't have correct permissions
chmod -R a+r jre
